"""BRW-026 Agent Scientific Tool Layer.

Agent → AgentToolRegistry → ToolGateway → WorkbenchService / IntakeEngine
→ deterministic scientific code. Tools are semantic, typed, evidence-aware,
and confirmation-aware; the layer itself never computes science.
"""
