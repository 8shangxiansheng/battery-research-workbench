services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: brw
      POSTGRES_PASSWORD: brw
      POSTGRES_DB: brw
    ports:
      - "5432:5432"
    volumes:
      - brw_postgres:/var/lib/postgresql/data

volumes:
  brw_postgres:
