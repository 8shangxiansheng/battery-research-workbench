先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-009/01_MASTER_PROMPT.md`。

执行 BRW-009 — Timestamp Construction Engine，但这一轮只做 Inspect，不修改代码。

请读取真实：

- BRW-008 `time_anchors.json`
- BRW-005 `frames.parquet`
- BRW-005 `parser_manifest.json`

并报告：

1. 当前 synchronization 目录已有哪些 BRW-008 模块；
2. `time_anchors.json` 的实际 schema 和 U001 selected anchor；
3. U001：
   - anchor_datetime
   - elapsed_time_s_at_anchor
   - anchor_status
   - anchor_source_type
   - timezone_known
4. frames.parquet：
   - row count
   - asset ids
   - first/last elapsed
   - 是否已有 `absolute_timestamp`
5. 用独立 datetime arithmetic 重新计算：
   - frame 0 timestamp
   - frame 1000 timestamp
   - frame 2000 timestamp
   - frame 3000 timestamp
   - frame 3998 timestamp
6. 比较 BRW-005 现有 `absolute_timestamp` 与上述 anchor-derived timestamp：
   - mismatch count
   - max absolute delta
7. elapsed 是否严格单调；
8. 是否有 duplicate elapsed；
9. 是否有 multi-asset / elapsed reset；
10. 推荐 ClockModel schema；
11. 推荐 timestamped_ultrasound_frames.parquet schema；
12. 推荐 timestamp_engine_manifest.json schema；
13. missing/rejected/conflicting anchor 应如何处理；
14. 如何保证 input/output row count 和 row order 不变；
15. 按 `05_TEST_PLAN.md` 建议新增哪些 tests；
16. 建议新增/修改哪些源码文件。

严格不要：

- 修改代码
- 读取 electrical records 做 row matching
- nearest timestamp matching
- 生成 sync_error_s
- cycle/step mapping
- drift fitting
- 把 timestamp 叫 verified sync
- 猜 timezone

输出 Inspect Report 后停止。
