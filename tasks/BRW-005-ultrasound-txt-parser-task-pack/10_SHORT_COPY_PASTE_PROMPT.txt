先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-005/01_MASTER_PROMPT.md`。

执行 BRW-005 — Ultrasound TXT Parser，但这一轮只做 Inspect，不修改代码。

请通过 `data/raw/manifests/data_assets.csv` 找到当前所有 `modality == ultrasound` 的 DataAsset，
并检查真实 TXT。

报告：

1. 当前 battery_id / experiment_id；
2. 找到了几个 Ultrasound DataAsset；
3. 每个 TXT 的文件名、SHA256、文件大小；
4. 每个 TXT 的非空行数/frame 数；
5. 每行 semicolon section 数；
6. frame_index_raw 起止值与是否连续；
7. elapsed_time_s 起止值、median interval、是否单调；
8. waveform sample count 是否固定；
9. unknown tail length 是否固定；
10. unknown_field_1 / unknown_meta pair 的取值形态，仅描述，不猜语义；
11. file_start_time 是否在 manifest 中可用；
12. sampling_rate_hz 是否有任何可靠来源；若没有明确说 UNKNOWN；
13. 当前 Data Contract 与真实 TXT 是否冲突；
14. 建议新增哪些 tests；
15. 建议新增/修改哪些代码文件。

不要：
- 修改代码
- 写 Zarr
- 做过滤/降噪
- 算 FFT
- 算 TOF
- 做 Electrical 同步
- 猜 unknown 字段
- 猜采样频率

输出 Inspect Report 后停止。
