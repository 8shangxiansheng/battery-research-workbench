先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-007/01_MASTER_PROMPT.md`。

执行 BRW-007 — Data Adapter Registry & Experiment Import Orchestrator，但这一轮只做 Inspect，不修改代码。

请报告：

1. 当前 `io/experiment/importer.py` 实际实现状态；
2. 当前 Electrical BRW-003 public service API；
3. 当前 Ultrasound BRW-005 public service API；
4. 当前 Battery/Experiment/DataAsset registry API；
5. 当前 `CELL_001 / EXP_001` 的 DataAssets；
6. 当前 processed electrical/ultrasound 输出是否已存在；
7. 建议的 DataAdapter interface；
8. 建议的 ModalityImportResult / ExperimentImportPlan / ExperimentImportResult schema；
9. 建议的 AdapterRegistry API；
10. ElectricalAdapter 应如何只包装现有 service；
11. UltrasoundAdapter 应如何只包装现有 service；
12. importer.py 当前哪些 placeholder 需要替换；
13. strict/non-strict 的推荐行为；
14. overwrite=False 的安全策略；
15. dry-run / plan 应如何实现；
16. 按 `tasks/BRW-007/05_TEST_PLAN.md` 建议新增哪些 tests；
17. 建议新增/修改哪些代码文件。

不要：
- 修改代码
- 修改 parser
- 修改 QA
- 做 synchronization
- 新增 modality
- 做 Agent/UI

输出 Inspect Report 后停止。
