先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-008/01_MASTER_PROMPT.md`。

执行 BRW-008 — Experiment Time Anchor Foundation，但这一轮只做 Inspect，不修改代码。

请报告：

1. 当前 synchronization 目录/placeholder 的实际状态；
2. 当前 Experiment / DataAsset 中所有与时间有关的字段；
3. 当前 `CELL_001 / EXP_001` experiment start/end；
4. 当前 E001 processed electrical absolute timestamp coverage；
5. electrical timestamp 是否有 duplicates，以及 BRW-008 为什么不能依赖 timestamp 唯一行；
6. 当前 U001：
   - file_start_time
   - elapsed min/max
   - first frame elapsed
   - last frame elapsed
7. 根据真实数据机械计算：
   - candidate first-frame absolute time
   - candidate last-frame absolute time
   - 相对 experiment/electrical start/end residual
   - overlap duration/fraction
8. 当前 timezone 是否有可靠来源；
9. 文件名 `export - 2024.01.06 - 21.03.01.txt` 中时间部分是否有正式格式合同；没有则标 UNKNOWN；
10. 推荐的 ExperimentTimeReference schema；
11. 推荐的 TimeAnchorCandidate / Evidence / Assessment schema；
12. 推荐 candidate resolution priority；
13. missing anchor 应如何处理；
14. multi-ultrasound asset / elapsed reset 应如何处理；
15. canonical `time_anchors.json` 应放在哪里；
16. 按 `05_TEST_PLAN.md` 建议新增哪些 tests；
17. 建议新增/修改哪些代码文件。

严格不要：

- 修改代码
- 把 manifest anchor 叫 verified sync
- 逐帧写 absolute timestamp
- electrical row matching
- cycle/step mapping
- drift fitting
- 从 filename 猜时间含义
- 猜 timezone

输出 Inspect Report 后停止。
