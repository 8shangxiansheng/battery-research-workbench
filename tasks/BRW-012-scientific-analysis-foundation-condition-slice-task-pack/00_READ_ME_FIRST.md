# BRW-012 — Scientific Analysis Foundation / Condition Slice Engine

放置到：

```text
battery-research-workbench/tasks/BRW-012/
```

执行：

```text
Inspect → Tests RED → Schemas → Conditions → Slice Engine
→ Persistence → Report → Real CELL_001 Validation → Final Gate
```

目标：

```text
measurement_events.parquet
↓
ConditionSliceSpec
↓
AnalysisSlice
```

本轮不做 waveform feature、TOF、FFT、ML、Agent、UI，也不重新同步。
