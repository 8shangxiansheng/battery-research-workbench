先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-004/01_MASTER_PROMPT.md`。

执行 BRW-004 — Electrical QA，但这一轮只做 Inspect，不修改代码。

请读取当前 BRW-003 已生成的：
- records.parquet
- cycles.parquet
- steps.parquet
- aux_temperature.parquet
- aux_voltage.parquet
- parser_manifest.json

报告：

1. battery_id / experiment_id；
2. records / cycles / steps / aux 行数；
3. timestamp range、duration、median interval、largest gap；
4. duplicate timestamp 数量与分组；
5. 核心字段 null count / null ratio；
6. voltage/current/capacity/temperature/SOC-DOD min/max；
7. 每个 cycle 的 records 数、charge/discharge capacity、时间区间；
8. 每个 cycle 的 step 顺序与时间区间；
9. records 与 cycles/steps/auxTemp/auxVol 是否明显不一致；
10. 按 `tasks/BRW-004/05_TEST_PLAN.md` 建议新增 tests；
11. 建议新增/修改源码文件。

不要：
- 修改 BRW-003 parser
- 删除 duplicate timestamps
- 清洗 processed Parquet
- 做 Ultrasound / Sync / ML / Agent / UI
- 直接开始实现

输出 Inspect Report 后停止。
