# Status and Quality Semantics

Experiment-level report:

```text
PASS
PASS_WITH_WARNINGS
FAIL
```

This is QA status for the matching run.

Separate scientific state:

```text
matching_performed = true
validated_sync = false
```

Current selected anchor is provisional,
so excellent nearest-match error does not independently prove clock synchronization.

Do not collapse these two concepts.
