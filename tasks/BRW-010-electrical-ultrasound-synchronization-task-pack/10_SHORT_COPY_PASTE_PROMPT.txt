先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-010/01_MASTER_PROMPT.md`。

执行 BRW-010 — Electrical–Ultrasound Synchronization，但这一轮只做 Inspect，不修改代码。

请读取真实：

- BRW-009 `timestamped_ultrasound_frames.parquet`
- BRW-003 electrical `records.parquet`
- 相关 manifests / parser manifests
- 当前 synchronization 源码

报告：

1. ultrasound timestamped frame row count、asset ids、timestamp min/max；
2. electrical record row count、asset ids、absolute timestamp column；
3. electrical 现有可用的稳定 record locator 字段；
4. electrical timestamp：
   - min/max
   - 是否单调
   - median positive interval
   - min/max positive interval
   - duplicate timestamp row count
   - duplicate timestamp group count
5. 列出所有 duplicate timestamp groups 的：
   - timestamp
   - record locators
   - cycle/step（仅作为诊断）
   - start/end marker（如存在）
6. 当前 ultrasound timestamps 是否全部落在 electrical coverage 内；
7. 用独立简单算法对 frame：
   - 0
   - 1000
   - 2000
   - 3000
   - 3998
   计算 nearest electrical timestamp、absolute error、candidate count；
8. 检查是否有 ultrasound frame 正好命中 duplicate electrical timestamp；
9. 检查是否存在等距 previous/next electrical timestamp tie；
10. 在 `max_sync_error_s=1.0` 下，先独立统计：
    - unique within tolerance
    - ambiguous within tolerance
    - out of tolerance
11. 推荐的 electrical record locator contract；
12. 推荐 `aligned_ultrasound_frames.parquet` schema；
13. 推荐 `synchronization_candidates.parquet` schema；
14. 推荐 boundary detection 逻辑；
15. 推荐 ambiguity type；
16. 当前 timezone compatibility；
17. 按 `05_TEST_PLAN.md` 建议新增哪些 tests；
18. 建议新增/修改哪些代码文件。

严格不要：

- 修改代码
- 自动选择 duplicate timestamp 的 first/last row
- 用 Cycle/Step 做 matching
- 做 interpolation
- 做 drift correction
- 生成 MeasurementEvent
- 把 provisional anchor 升级成 verified sync

输出 Inspect Report 后停止。
