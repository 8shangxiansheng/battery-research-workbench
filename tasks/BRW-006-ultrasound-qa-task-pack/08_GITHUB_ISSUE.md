# BRW-006: Ultrasound QA

Build deterministic structural, temporal, waveform and cross-frame QA over BRW-005 outputs.

Outputs: QA JSON/HTML、frame_quality.csv、asset_summary.csv、anomalies.csv、8 QA figures。

Out of scope: preprocessing, TOF, physical FFT features, synchronization, ML, Agent, UI。
