先阅读仓库根目录 `AGENTS.md`，然后阅读 `tasks/BRW-006/01_MASTER_PROMPT.md`。

执行 BRW-006 — Ultrasound QA，但这一轮只做 Inspect，不修改代码。

读取当前 `frames.parquet / waveforms.zarr / parser_manifest.json`，报告：
1. battery/experiment/asset；
2. metadata frame count、Zarr shape、dtype；
3. elapsed 起止、median/min/max interval、monotonicity、large gaps；
4. waveform global min/max；
5. RMS min/median/max；
6. P2P min/median/max；
7. waveform mean/DC min/median/max；
8. all-zero / constant frame count；
9. adjacent Pearson correlation min/median/max；
10. 最低相关 10 个 frame；
11. RMS/P2P/DC 最大跳变 frame；
12. repeated extreme plateau 是否存在，只描述 possible saturation；
13. sampling_rate_hz 是否仍 UNKNOWN；
14. 当前 QA 风险；
15. 建议 tests；
16. 建议源码文件。

不要修改代码，不滤波，不 alignment，不算 TOF，不输出 Hz/MHz，不建正式 feature dataset，不做 Electrical 同步/ML/Agent/UI。输出 Inspect Report 后停止。
